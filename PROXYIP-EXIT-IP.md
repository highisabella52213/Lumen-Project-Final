# ProxyIP اختصاصی هر کانفیگ — v13

## محل تنظیم

در پنل وارد **Limited configs → Route Studio** شوید و در کارت **ProxyIP for this config** یکی از حالت‌ها را انتخاب کنید:

- **Direct**: اتصال مستقیم و پیش‌فرض.
- **ProxyIP**: استفاده از ProxyIP فقط برای همان کانفیگ/UUID.

این تنظیم از Settings حذف شده است تا یک ProxyIP خراب روی کاربران دیگر اثر نگذارد. در ویرایش هر کانفیگ نیز همین فیلدها در دسترس‌اند.

## ایمنی اتصال

ProxyIP در v13 همیشه **fail-open** است. اگر آدرس نامعتبر، خارج از دسترس، blackhole، کند یا غیرقابل‌تأیید باشد، کانفیگ قطع نمی‌شود و پس از بودجه‌ی زمانی محدود مستقیم وصل می‌شود. گزینه‌ای برای خاموش‌کردن این fallback وجود ندارد.

ProxyIP معکوس تنها وقتی امتحان می‌شود که بسته‌ی اول یک رکورد کامل TLS باشد. اگر ClientHello در فریم بعدی WS شکسته شده باشد، سرور فقط برای ۰٫۵۵ ثانیه و فقط برای کانفیگ دارای ProxyIP آن را جمع می‌کند. بسته‌های خالی، غیر-TLS یا ناقص مستقیم می‌روند. هیچ بایتی دوبار ارسال یا از سهمیه دوبار کم نمی‌شود.

## فرمت‌ها

ورودی‌ها با کاما، فاصله یا خط جدید جدا می‌شوند؛ حداکثر ۸ کاندید نهایی استفاده می‌شود:

```text
proxy.example.com
proxy.example.com:8443
1.2.3.4
[2606:4700::1]:2053
ts.example.com.tp8443
```

تعداد بررسی موازی بین ۱ تا ۶ است. ترتیب کاندیدها بر پایه‌ی UUID و دامنه‌ی مقصد قطعی است تا برای یک کاربر/مقصد پایدار بماند.

## زمان‌ها

- `PROXYIP_VERIFY_TIMEOUT`: انتظار اولین بایت واقعی پاسخ، پیش‌فرض ۶ ثانیه.
- `PROXYIP_TOTAL_TIMEOUT`: سقف کل تلاش ProxyIP، پیش‌فرض ۶ ثانیه.

بعد از این سقف، اتصال مستقیم انجام می‌شود. کیفیت و سرعت خروجی به ProxyIP بیرونی وابسته است.

## پیش‌فرض‌های Address و SNI

| مقدار | Address | SNI |
|---|---:|---:|
| `railway.com` | بله | بله |
| `69.46.46.18` | بله | خیر |
| `69.46.46.126` | بله | خیر |

IP هیچ‌وقت به‌عنوان SNI پیشنهاد نمی‌شود. دامنه‌ی جاری سرویس و مقادیر `VLESS_ADDRESSES` / `VLESS_SNI_NAMES` نیز افزوده می‌شوند.
