# Lumen · WS-only v13

## تغییرات اصلی

- انتقال کامل ProxyIP از Settings به ساخت و ویرایش هر کانفیگ.
- جداسازی ProxyIP بر اساس همان UUID؛ تنظیم خراب یک کاربر روی دیگری اثر ندارد.
- بازسازی مسیر ProxyIP با تأیید اولین بایت واقعی، بودجه‌ی کل محدود و بازگشت مستقیم اجباری.
- جمع‌آوری کوتاه ClientHello شکسته‌شده بین فریم‌های WS بدون ارسال یا حسابداری تکراری.
- ترتیب پایدار کاندیدها بر اساس UUID و دامنه مقصد و بررسی موازی ۱ تا ۶.
- افزودن `railway.com` به Address و SNI؛ افزودن `69.46.46.18` و `69.46.46.126` فقط به Address.
- حذف توضیح رله/WS/VLESS از صفحه عمومی و نمایش خنثی سلامت سامانه.
- کارت وضعیت Direct/ProxyIP در هر route، ترجمه فارسی/انگلیسی و رنگ‌های semantic در تم روشن/تیره.

## آزمون‌ها

- ۱۵ تست کامل: صفر شکست.
- ProxyIP سالم: خروجی مقصد از IP رله‌ی تست دیده شد.
- ProxyIP خراب و blackhole: اتصال مستقیم برقرار شد؛ بدون قطع کانفیگ.
- جداسازی A/B در سه چرخه، non-TLS bypass، split ClientHello، ترتیب قطعی و نبود task leak.
- ۶۰ رندر پنل/لندینگ در فارسی/انگلیسی، روشن/تیره و دسکتاپ/موبایل: صفر overflow، کنترل بیرون صفحه یا دکمه کوچک.

نتیجه‌ی throughput تست loopback تضمین سرعت اینترنت نیست؛ سرعت واقعی به مسیر شبکه و ProxyIP وابسته است.
