# EMS11 WS-only v10

این نسخه فقط VLESS روی WebSocket را ارائه می‌کند. ترابرد HTTP چندحالته، مسیرهای سرور، لینک‌ساز، گزینه‌های پنل، تست‌ها و فایل پیاده‌سازی آن حذف شده‌اند.

## مسیر و لینک

- مسیر: `/ws/{uuid}`
- لینک تولیدشده: `type=ws`
- Early Data: `ed=4096`
- ALPN پیش‌فرض: `http/1.1`
- کانفیگ‌های قدیمی هنگام بارگذاری state خودکار به `vless-ws` مهاجرت می‌کنند.

## بهینه‌سازی‌های فعال WS

- Uvicorn Sans-I/O سفارشی و ارسال payload بدون کپی اضافه
- صف Burst با backpressure حجمی و تعدادی
- خواندن تطبیقی و کم‌کپی از `StreamReader`
- Cache کردن متدهای پرتکرار داخل حلقهٔ انتقال برای کاهش lookup پایتون در هر فریم
- حسابرسی حجمی دسته‌ای، بدون قفل سراسری در هر فریم
- DNS cache، Happy Eyeballs واقعی، مسابقهٔ IPv4/IPv6 و حافظهٔ مسیر
- `TCP_NODELAY`، `TCP_QUICKACK`، BBR/Cubic fallback و بافرهای بزرگ ولی محدود
- WebSocket compression خاموش، چون ترافیک رمز‌شده فشرده نمی‌شود و فقط CPU مصرف می‌کند
- ping داخلی خودکار خاموش و Early Data برای کاهش زمان شروع

## نکتهٔ عملکردی

دو پروفایل تهاجمی‌تر با فریم ۱۶ MiB، StreamReader برابر ۶۴ MiB و Burst بزرگ‌تر آزمایش شدند، اما در بنچمارک از پروفایل فعلی کندتر بودند؛ بنابراین حذف شدند. اعداد loopback تضمین سرعت اینترنت نیستند.
