# WS Hyper Dual-Stack — گزارش نسخه نهایی

این مرحله فقط روی WebSocket کار کرده است؛ منطق XHTTP تغییر نکرده است.

## تغییرات مسیر شبکه

- Listener اصلی ابتدا روی `::` با `IPV6_V6ONLY=0` بالا می‌آید و IPv6 و IPv4 را هم‌زمان می‌پذیرد. اگر کرنل dual-stack را تضمین نکند، خودکار به listener قبلی IPv4 برمی‌گردد؛ بنابراین IPv4 قربانی IPv6 نمی‌شود.
- اتصال مقصد با مدل Adaptive Happy Eyeballs انجام می‌شود: اولین IPv6 و اولین IPv4 بدون تأخیر و هم‌زمان شروع می‌شوند؛ سریع‌ترین مسیر برنده می‌شود.
- پاسخ‌های DNS به‌صورت IPv6/IPv4 درهم‌تنیده می‌شوند و ترتیب RFC 6724 سیستم‌عامل حفظ می‌شود؛ دیگر IPv4 به‌طور اجباری بالاتر از همه IPv6ها قرار نمی‌گیرد.
- مسیر موفق، RTT و خطاهای هر آدرس در cache ثبت می‌شوند. مسیر خراب cooldown نمایی می‌گیرد، اما بعداً دوباره امتحان می‌شود تا recovery ممکن باشد.
- IPv6 دارای scope-id نیز حفظ می‌شود و `IPV6_TCLASS` برای مسیر IPv6 اعمال می‌شود.
- مقصد تک‌آدرس از fast path بدون ساخت taskهای مسابقه‌ای اضافه عبور می‌کند تا first-byte صفحات کوتاه سریع بماند.

## تغییرات سرعت WS

- ارسال دانلود در پروتکل سفارشی Uvicorn، وقتی compression خاموش است، هدر استاندارد RFC 6455 و همان payload موجود را جداگانه می‌نویسد. کپی کامل BytesIO برای هر فریم چندمگابایتی حذف شده است.
- parsing ورودی، mask validation، ping/close و state machine همچنان در موتور رسمی Sans-I/O `websockets` باقی مانده‌اند.
- صف آپلود علاوه بر 256/64 پیام، سقف بایتی 64/16 MiB دارد؛ throughput بالا حفظ می‌شود ولی queue نمی‌تواند بی‌حد حافظه مصرف کند.
- بافر سوکت تا 32 MiB، StreamReader تا 32 MiB و write-window تطبیقی از 8 تا 64 MiB تنظیم شده‌اند.
- `TCP_NODELAY`، `TCP_QUICKACK` و BBR با fallback خودکار CUBIC فعال می‌شوند؛ همه best-effort هستند.
- accounting هر فریم از coroutine اضافه خارج شده و فقط هنگام کامل‌شدن batch واقعاً await می‌کند.
- Early Data روی `ed=4096`، فشرده‌سازی WS خاموش، `uvloop` و `httptools` حفظ شده‌اند.

## Mux: روشن یا خاموش؟

برای پروفایل اصلی سرعت، **Mux خاموش بماند**. مستندات رسمی Xray می‌گوید Mux برای کم‌کردن handshake latency است و در دانلود، ویدئو و speed-test معمولاً اثر منفی دارد. Mux چند جریان را روی یک TCP/congestion-window می‌گذارد و برای یک دانلود سنگین سرعت جادویی ایجاد نمی‌کند.

برای شبکه‌های پینگ‌بالا می‌توان پروفایل جداگانه Web-Boost با `enabled=true` و `concurrency=4` آزمایش کرد. تنظیم دقیق در `XRAY-WS-PROFILES.md` آمده است. Mux فقط سمت کلاینت تنظیم می‌شود و در لینک استاندارد VLESS قابل اجبار نیست.

## تست‌های نهایی این محیط

- صحت normal / split-header / Early Data: پاس
- یک جریان 256 MiB: **598.1 MiB/s در هر جهت** روی loopback
- 32 جریان هم‌زمان × 8 MiB: **236.3 MiB/s در هر جهت** روی loopback
- 60 اتصال کوتاه × 16 KiB: **626.5 درخواست/ثانیه**، p50 برابر **92.54 ms**
- IPv6 واقعی روی `::1`: پاس
- IPv6 خراب و fallback هم‌زمان IPv4: **0.5 ms** در تست قطعی loopback
- 8 چرخه × 24 اتصال: active=0، errors=0، FD از 9 به 9، task باقی‌مانده=0
- تست framing برای 512 MiB: کپی payload از یک بافر 8 MiB اضافه به صفر رسید؛ این microbenchmark مسیر framing است، نه تضمین سرعت اینترنت.
- اجرای دقیق `python main.py` با alias رفع circular-import: پاس

## نکات واقع‌بینانه

- عدد loopback تضمین سرعت اینترنت نیست؛ ISP، RTT، packet loss، توان CDN و محدودیت میزبان مهم‌ترند.
- یک جریان استاندارد WS/TCP را سرور نمی‌تواند بدون پشتیبانی کلاینت به چند اتصال تقسیم کند. برای دانلود واقعی، download manager با 8 تا 16 Range connection معمولاً بهتر از Mux است.
- BBR فقط وقتی واقعاً استفاده می‌شود که کرنل میزبان آن را داشته باشد؛ در غیر این صورت CUBIC فعال است.
- پورت عمومی دامنه/TLS برابر `443` و پورت داخلی برنامه همان `$PORT` (پیش‌فرض 8000) است.
